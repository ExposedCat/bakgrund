{
  "tasks": {
    "start": "deno run --allow-run=busctl --allow-net main.ts",
    "dev": "deno run --watch --allow-run=busctl --allow-net main.ts",
    "check": "deno fmt --check && deno lint && deno check main.ts"
  }
}
