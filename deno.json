{
  "tasks": {
    "start": "deno run --allow-run=busctl --allow-net src/main.ts",
    "dev": "deno run --watch --allow-run=busctl --allow-net src/main.ts",
    "check": "deno fmt --check && deno lint && deno check src/main.ts"
  }
}
